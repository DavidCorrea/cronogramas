# spec — Tests

**Jest** + **ts-jest**. Test descriptions should describe real user/product scenarios; avoid technical jargon. TDD for new features: add or update tests here when implementing behaviour.

Run: `npm test` (single run), `npm run test:watch` (watch mode). Pre-commit does not run tests; run manually before pushing. See **AGENTS.md** (Testing) for conventions.
